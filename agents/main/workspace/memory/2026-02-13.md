# Memory - 2026-02-13

## Harpy Update Agent
- Created agent `harpy` (ID: harpy) with tools: exec, gateway, cron, message
- Purpose: Nightly automated maintenance (Linux updates, OpenClaw update, skills, plugins)
- Workspace: `~/.openclaw/workspace-harpy`
- SOUL.md defines update sequence and reporting
- Cron job scheduled: `0 3 * * *` America/New_York → isolated harpy session, announces to webchat
- Job ID: b6ecb66e-30e9-413a-8a18-8ae8a95a137d
- One-off test job created (delete-after-run)

## Security Audit Conducted
Critical findings:
- Extensions not allowlisted → fix: set `plugins.allow` to include `openclaw-supermemory`
- Gateway exposed on 0.0.0.0:18789 → fix: bind to localhost or firewall block
- Firewall inactive → enable UFW with deny-by-default, allow SSH and chosen services
- Auth-profiles.json (roci) world-readable → chmod 600
- Unnecessary services on ports 3000, 25 → identify and disable
- OpenClaw update available → run `openclaw update`
- SSH password auth enabled → switch to key-only after verifying access
- Running as root → consider non-root user migration

Warnings:
- Gateway service PATH issues → `openclaw doctor --repair`

## Actions Taken
- Harpy agent fully created and registered
- Auth-profiles permissions fixed on roci (644→600) and harpy created with 600
- OpenClaw config updated to include harpy in agents.list
- Nightly cron job added at 3 AM ET

## Next Steps (Remediation Plan)
1. Set plugins.allow and restart gateway
2. Apply OpenClaw update
3. Configure UFW firewall
4. Bind gateway to localhost OR confirm remote access needed
5. Disable SSH password auth after key verification
6. Stop services on port 3000 and 25
7. Run `openclaw doctor --repair`
8. Consider non-root migration
