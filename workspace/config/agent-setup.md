# Agent Configuration Snapshot

This document captures static agent configuration that is unlikely to change frequently. For dynamic learned information, see `MEMORY.md`.

## Default Agent

- **Ciel** is the default agent (set 2026-02-11)

## Delegation

- No subagents currently configured

## Models

- Ciel and Juno use `openrouter/stepfun/step-3.5-flash:free` as primary model
- No `main` agent defined; agents live in `agents/<id>` directories under workspace
